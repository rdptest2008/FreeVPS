name: Terminal Only

on:
  workflow_dispatch:

jobs:
  open-terminal:
    name: Open Terminal
    runs-on: ubuntu-latest

    steps:
      - name: Setup tmate session
        uses: mxschmitt/action-tmate@v3
